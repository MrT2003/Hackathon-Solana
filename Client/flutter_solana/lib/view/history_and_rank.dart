import 'package:flutter/material.dart';

class HistoryAndRank extends StatelessWidget {
  const HistoryAndRank({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("History and Rank"),
      ),
    );
  }
}
